// Package plugins hosts the runtime side of GoModel's plugin system: the
// catalog of plugin types, configured instances, per-phase chains and the
// Host implementation handed to plugins. It maps between core request types
// and the pluginapi contract through the exchange subpackage.
package plugins
