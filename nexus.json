{
  "name": "NEXUS AI Gateway",
  "description": "OpenAI-compatible AI gateway with a dashboard, audit logs and spend control.",
  "env": {
    "PORT": { "value": "8080" },
    "STORAGE_TYPE": { "value": "sqlite" },
    "ADMIN_BOOTSTRAP_USERNAME": { "required": true, "description": "Admin UI USERNAME." },
    "ADMIN_BOOTSTRAP_PASSWORD": { "required": true, "description": "Admin UI PASSWORD." },
    "OPENAI_API_KEY": { "description": "Optional" },
    "ANTHROPIC_API_KEY": { "description": "Optional" },
    "GEMINI_API_KEY": { "description": "Optional" },
    "GROQ_API_KEY": { "description": "Optional" }
  }
}
